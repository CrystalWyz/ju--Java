<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wyz.user.mapper.GroupMemberMapper">
    <resultMap type="cn.wyz.user.bean.GroupMember" id="GroupMemberMap">
        <result property="id" column="id"
                jdbcType="INTEGER"/>
        <result property="createTime" column="create_time"
                jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time"
                jdbcType="TIMESTAMP"/>
        <result property="createdBy" column="created_by"
                jdbcType="VARCHAR"/>
        <result property="lastModifiedBy" column="last_modified_by"
                jdbcType="VARCHAR"/>
        <result property="groupId" column="group_id"
                jdbcType="INTEGER"/>
        <result property="userId" column="user_id"
                jdbcType="INTEGER"/>
        <result property="roleType" column="role_type"
                jdbcType="INTEGER"/>
        <result property="status" column="status"
                jdbcType="INTEGER"/>
        <result property="joinedAt" column="joined_at"
                jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="column">
        `id`,
 
 `create_time`,
 
 `update_time`,
 
 `created_by`,
 
 `last_modified_by`,
 
 `group_id`,
 
 `user_id`,
 
 `role_type`,
 
 `status`,
 
 `joined_at`
    </sql>

    <sql id="where">
        <where>
            <if test="id != null">
                and `id` = #{id}
            </if>
            <if test="createTime != null">
                and `create_time` = #{createTime}
            </if>
            <if test="updateTime != null">
                and `update_time` = #{updateTime}
            </if>
            <if test="createdBy != null">
                and `created_by` = #{createdBy}
            </if>
            <if test="lastModifiedBy != null">
                and `last_modified_by` = #{lastModifiedBy}
            </if>
            <if test="groupId != null">
                and `group_id` = #{groupId}
            </if>
            <if test="userId != null">
                and `user_id` = #{userId}
            </if>
            <if test="roleType != null">
                and `role_type` = #{roleType}
            </if>
            <if test="status != null">
                and `status` = #{status}
            </if>
            <if test="joinedAt != null">
                and `joined_at` = #{joinedAt}
            </if>
            <if test="sorts != null and sorts.size > 0)">
                order by
                <foreach collection="sorts" item="sort" separator=", ">
                    `#{sort.column}` #{sort.sort}
                </foreach>
            </if>

            <if test="size != null and current != null">
                limit #{current},#{size}
            </if>
        </where>
    </sql>

    <!-- 软删除 -->
    <delete id="remove">
        update ju.group_member
        set `deleted` = 1
        <include refid="where"/>
    </delete>

    <!-- 分页查询 -->
    <select id="pageQuery" resultMap="GroupMemberMap">
        select
        <include refid="column"/>
        from group_member
        <include refid="where"/>
    </select>
</mapper>

